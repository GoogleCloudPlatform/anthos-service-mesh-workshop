apiVersion: "networking.istio.io/v1alpha3"
kind: "DestinationRule"
metadata:
  name: "redis-carts-cart-destrule"
  namespace: "cart"
spec:
  host: "redis-carts.cart.svc.cluster.local"
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
